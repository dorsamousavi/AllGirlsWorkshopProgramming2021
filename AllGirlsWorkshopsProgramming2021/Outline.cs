/*
 * functions:
 * Console.WriteLine("message")
 *      prints message to terminal
 * Console.ReadLine()
 *      returns the user's input as a string value 
 * 
 * Syntax:
 * Assigning a value to a variable 
 *      variableType variableName = value 
 * 
 */
using System;

namespace AllGirlsWorkshopsProgramming20211
{
    class ChatBot
    {
        static void Main(string[] agrs)
        {
            //print out an inital question

            //assign the user's input to a variable named answer with a string type
            string answer = Console.ReadLine();

            //use a chain of if and else if statments to analyze the user's response and respond based on the user's input
            if (answer == "")//test if answer is equal to a certain response
            {
                //print a response to the user's input 

            }
            else if(answer == "")//test if answer is equal to a certain response
            {
                //print a response to the user's input
            }
            else if (answer == "") //test if answer is equal to a certain response
            {
                //print a response to the user's input
            }
            else // if the user's repsonse does not match any of your checked responses 
            {
                //print a response to the user's input
            }
        }
    }
}
